'use client'

export default function Heading(){
    const heading = `text-[60px] mt-[206px] mb-24 font-semibold max-w-full px-[300px] text-center`

    return(
        <div><h1 className={heading} id="head">We enrich human lives through the thoughtful application of design and technology</h1>
        </div>
    )
}